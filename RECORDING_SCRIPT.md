# 海潮 Web Demo 录屏脚本（3–5 分钟）

## 录制前准备

- 浏览器打开 `http://localhost:4173` 或你部署后的 Web Demo 链接。
- 窗口建议 1440×900 或全屏，缩放 100%。
- 如果未配置 API 密钥，页面会显示演示模式；如果已配置 `.env`，页面会显示 API 实时模式。
- 不要录制或展示 `.env`、API 密钥、账号密码、Cookie。

## 分镜脚本

### 片段 1：高级感首屏与项目定位（0:00–0:25）

1. 停留在首屏 Hero，不操作 3 秒，让动态海潮背景和中心卡片自然运动。
2. 鼠标轻轻移动到右侧卡片，展示卡片倾斜和光效。
3. 旁白：这是「海潮」，面向福州海洋文化、直播电商与水产品外贸的可信内容智能体。
4. 镜头停留核心指标：3 个专业 Agent、24 个节点、11 层可信链路。

### 片段 2：价值说明（0:25–0:55）

1. 点击顶部「价值」或向下滚动到“为什么需要海潮”。
2. 停留标题“把会写文案升级为敢交付的内容系统”约 3 秒。
3. 依次扫过四张卡片：证据感知、跨场景复用、福州落地、低门槛输入。
4. 旁白：海潮的重点不是生成更多文字，而是让事实、商品状态和业务缺口可检查。

### 片段 3：文化内容演示（0:55–1:35）

1. 点击顶部「体验」。
2. 左侧选择「马尾船政文化短视频」。
3. 保留默认输入：为马尾船政文化生成 30 秒抖音口播脚本和 6 镜头分镜。
4. 点击「生成可信内容」。
5. 等待动效结束后，停留在输出区 10–15 秒。
6. 重点展示「30秒口播脚本」「6镜头分镜」「证据/边界说明」「发布前补充」。

### 片段 4：电商内容演示（1:35–2:10）

1. 点击左侧「连江鲍鱼直播话术」。
2. 点击「生成可信内容」。
3. 结果出现后，鼠标指向“保留用户明确字段”和“不虚构库存、发货、功效和促销”。
4. 旁白：商品已上架、规格和价格已知时，海潮保留这些字段；没有给到的优惠和库存不会擅自补。

### 片段 5：外贸内容演示（2:10–2:50）

1. 点击左侧「水产品外贸询盘」。
2. 点击「生成可信内容」。
3. 停留在金额计算：1,000 KG × 45 USD/KG = USD 45,000。
4. 展示“买卖方、贸易术语、运输信息未提供”进入发布前补充。
5. 旁白：外贸场景强调可复核字段，不虚构 HS 编码、港口和认证。

### 片段 6：三域协同与架构（2:50–3:45）

1. 点击「三域协同任务」。
2. 点击「生成可信内容」，展示文化、电商、外贸同时出现。
3. 向下滚动到「可信系统结构」。
4. 停留展示：自然语言输入 → 三专家线 → 事实闸门 → 发布闸门 → 业务成品。
5. 旁白：它不是单点工具，而是端到端可信内容链路。

### 片段 7：收尾与提交口径（3:45–4:20）

1. 回到 Demo 输出区或页面底部录屏路线。
2. 点击「复制」，展示结果可直接进入业务编辑流程。
3. 旁白：Web Demo 作为体验链接提交，HiAgent API 地址和 APPID 可在备注中填写；API 密钥不公开提交。

## 推荐提交口径

- 体验链接 / Demo：填写部署后的 Web Demo 链接；若还未部署，先使用录屏展示本地 Web Demo。
- API：填写 `http://14.103.118.162:32300/api/proxy/api/v1`，备注 APPID：`d9m72hpb9rsa732fojg0`，版本：`v5.2.0`。
- 录屏：优先录 Web Demo；如需证明平台真实状态，再补 10–15 秒 HiAgent 发布页，但不要展示 API 密钥。
'@ | apply_patch
